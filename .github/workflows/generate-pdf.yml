name: Generate PDF CV

on:
  push:
    branches: [ main, master ]
  workflow_dispatch: # Allow manual trigger

jobs:
  generate-pdf:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write  # Required for pushing changes back
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
      
    - name: Setup Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.1'
        bundler-cache: true
        
    - name: Build Jekyll site
      run: |
        echo "🔨 Building Jekyll site..."
        bundle exec jekyll build --destination _site --verbose
        
        echo "✅ Jekyll build completed"
        echo "📁 Checking _site directory structure:"
        if [ -d "_site" ]; then
          find _site -type f -name "*.html" | head -20
          echo "📊 Total HTML files: $(find _site -name "*.html" | wc -l)"
        else
          echo "❌ ERROR: _site directory was not created!"
          exit 1
        fi
        
    - name: Verify print page exists
      run: |
        echo "🔍 Looking for print page..."
        
        PRINT_FOUND=false
        PRINT_PATH=""
        
        # Check for print/index.html
        if [ -f "_site/print/index.html" ]; then
          echo "✅ Found print page at: _site/print/index.html"
          PRINT_FOUND=true
          PRINT_PATH="_site/print/index.html"
        fi
        
        # Check for print.html
        if [ -f "_site/print.html" ]; then
          echo "✅ Found print page at: _site/print.html"
          PRINT_FOUND=true
          PRINT_PATH="_site/print.html"
        fi
        
        if [ "$PRINT_FOUND" = false ]; then
          echo "❌ ERROR: Print page not found!"
          echo "📁 Available files in _site:"
          ls -la _site/ || echo "Cannot list _site directory"
          echo "📁 Looking for any print-related files:"
          find _site -name "*print*" -type f || echo "No print-related files found"
          exit 1
        fi
        
        echo "PRINT_PATH=$PRINT_PATH" >> $GITHUB_ENV
        
    - name: Generate PDF using playwright
      run: |
        echo "🎭 Starting PDF generation with Playwright..."
        
        # Install playwright with verbose output
        echo "📦 Installing Playwright..."
        npm install playwright --verbose || {
          echo "❌ Failed to install Playwright"
          exit 1
        }
        
        echo "🔧 Installing Playwright browsers..."
        npx playwright install chromium --with-deps || {
          echo "❌ Failed to install Playwright browsers"
          exit 1
        }
        
        echo "✅ Playwright installation completed"
        
        # Test Node.js and Playwright
        echo "🧪 Testing Node.js and Playwright..."
        node --version
        npm list playwright
        
        # Create and run PDF generation script
        echo "📝 Creating PDF generation script..."
        
        node -e "
        console.log('🚀 Node.js script started');
        
        const { chromium } = require('playwright');
        const fs = require('fs');
        const path = require('path');
        
        async function generatePDF() {
          let browser;
          let context;
          let page;
          
          try {
            console.log('🌐 Launching Chromium browser...');
            browser = await chromium.launch({
              headless: true,
              args: ['--no-sandbox', '--disable-setuid-sandbox', '--disable-dev-shm-usage']
            });
            console.log('✅ Browser launched successfully');
            
            console.log('📄 Creating browser context and page...');
            context = await browser.newContext({
              viewport: { width: 1200, height: 800 }
            });
            page = await context.newPage();
            console.log('✅ Page created successfully');
            
            // Find print file
            const printPath = process.env.PRINT_PATH;
            console.log('📂 Print path from env:', printPath);
            
            if (!printPath) {
              throw new Error('PRINT_PATH environment variable not set');
            }
            
            const fullPath = path.resolve(printPath);
            console.log('📂 Full print file path:', fullPath);
            
            if (!fs.existsSync(fullPath)) {
              console.log('❌ Print file does not exist at:', fullPath);
              console.log('📁 Current directory contents:');
              fs.readdirSync('.').forEach(item => console.log('  -', item));
              if (fs.existsSync('_site')) {
                console.log('📁 _site directory contents:');
                fs.readdirSync('_site').forEach(item => console.log('  -', item));
              }
              throw new Error('Print file does not exist: ' + fullPath);
            }
            
            const fileStats = fs.statSync(fullPath);
            console.log('📊 Print file size:', fileStats.size, 'bytes');
            
            // Read file content for debugging
            const fileContent = fs.readFileSync(fullPath, 'utf8');
            console.log('📝 File content length:', fileContent.length);
            console.log('📝 File content preview:', fileContent.substring(0, 200) + '...');
            
            // Load the page
            const fileUrl = 'file://' + fullPath;
            console.log('🔗 Loading URL:', fileUrl);
            
            const response = await page.goto(fileUrl, {
              waitUntil: 'networkidle',
              timeout: 60000
            });
            
            console.log('✅ Page loaded, response status:', response ? response.status() : 'no response');
            
            // Wait for content to load
            console.log('⏳ Waiting for page content...');
            await page.waitForTimeout(3000);
            
            // Check page content
            const title = await page.title();
            console.log('📄 Page title:', title);
            
            const bodyContent = await page.textContent('body');
            console.log('📝 Body content length:', bodyContent ? bodyContent.length : 0);
            
            if (!bodyContent || bodyContent.trim().length === 0) {
              console.log('❌ Page appears to be empty');
              const htmlContent = await page.content();
              console.log('🔍 Full HTML content:', htmlContent.substring(0, 1000));
              throw new Error('Page appears to be empty');
            }
            
            // Generate PDF
            console.log('📄 Generating PDF...');
            const pdf = await page.pdf({
              format: 'A4',
              printBackground: true,
              margin: { 
                top: '0.5in', 
                right: '0.5in', 
                bottom: '0.5in', 
                left: '0.5in' 
              }
            });
            console.log('✅ PDF generated, size:', pdf.length, 'bytes');
            
            // Ensure output directory exists
            const outputDir = 'assets/pdf';
            if (!fs.existsSync(outputDir)) {
              console.log('📁 Creating directory:', outputDir);
              fs.mkdirSync(outputDir, { recursive: true });
            }
            
            // Save PDF
            const outputPath = 'assets/pdf/cv-keg-dev-print.pdf';
            console.log('💾 Saving PDF to:', outputPath);
            fs.writeFileSync(outputPath, pdf);
            
            const savedStats = fs.statSync(outputPath);
            console.log('✅ PDF saved successfully:', Math.round(savedStats.size / 1024), 'KB');
            
          } catch (error) {
            console.error('❌ PDF generation failed:');
            console.error('Error name:', error.name);
            console.error('Error message:', error.message);
            console.error('Error stack:', error.stack);
            process.exit(1);
          } finally {
            if (page) {
              console.log('🧹 Closing page...');
              await page.close().catch(e => console.error('Error closing page:', e.message));
            }
            if (context) {
              console.log('🧹 Closing context...');
              await context.close().catch(e => console.error('Error closing context:', e.message));
            }
            if (browser) {
              console.log('🧹 Closing browser...');
              await browser.close().catch(e => console.error('Error closing browser:', e.message));
            }
            console.log('✅ Cleanup completed');
          }
        }
        
        generatePDF().catch(error => {
          console.error('❌ Unhandled error in generatePDF:', error);
          process.exit(1);
        });
        " || {
          echo "❌ Node.js script execution failed"
          exit 1
        }
        
    - name: Verify PDF was created
      run: |
        echo "🔍 Verifying PDF creation..."
        
        if [ -f "assets/pdf/cv-keg-dev-print.pdf" ]; then
          PDF_SIZE=$(stat -c%s "assets/pdf/cv-keg-dev-print.pdf")
          echo "✅ PDF created successfully!"
          echo "📊 PDF file size: $PDF_SIZE bytes ($(($PDF_SIZE / 1024)) KB)"
          
          if [ $PDF_SIZE -lt 1000 ]; then
            echo "⚠️  WARNING: PDF file seems very small, might be corrupted"
          fi
        else
          echo "❌ ERROR: PDF file was not created!"
          echo "📁 Contents of assets/pdf directory:"
          ls -la assets/pdf/ || echo "assets/pdf directory does not exist"
          exit 1
        fi
        
    - name: Commit and push updated PDF
      run: |
        echo "📤 Committing and pushing PDF..."
        
        git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        
        # Check if there are changes to commit
        if git diff --quiet assets/pdf/cv-keg-dev-print.pdf; then
          echo "ℹ️  No changes detected in PDF file"
        else
          echo "📝 Changes detected in PDF file"
        fi
        
        git add assets/pdf/cv-keg-dev-print.pdf
        
        if git commit -m "🤖 Auto-update CV PDF [skip ci]"; then
          echo "✅ PDF committed successfully"
          git push
          echo "✅ Changes pushed to repository"
        else
          echo "ℹ️  No changes to commit"
        fi